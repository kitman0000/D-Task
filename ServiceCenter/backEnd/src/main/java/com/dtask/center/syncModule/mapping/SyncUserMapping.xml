<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dtask.center.syncModule.dao.SyncUserDao">

    <insert id="insertUserInfo">
        REPLACE INTO tb_user_info
        <set>
            id = #{userListEntity.id},
            nodeID = #{nodeID},
            nickname = #{userListEntity.nickname},
            email = #{userListEntity.email},
            onboardDate = #{userListEntity.onboardDate},
            birthday = #{userListEntity.birthDay},
            lastUpdateTime = #{userListEntity.lastUpdateTime},
            available = #{userListEntity.available}
        </set>
    </insert>
</mapper>